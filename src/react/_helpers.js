/**
 * MUI React helpers
 * @module react/_helpers
 */

'use strict';


let controlledMessage = 'You provided a `value` prop to a form field ' +
  'without an `OnChange` handler. Please see React documentation on ' +
  'controlled components';


module.exports = { controlledMessage };
