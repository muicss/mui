describe('react/forms.jsx', function() {
  var assert = require('assert'),
      helpers = require('../helpers.js'),
      forms = require('../../src/react/forms.jsx');

  helpers.initDOM();

  // --------------------
  // 
  // --------------------
  describe('blah', function() {
    it('should blah', function() {
      assert.equal(true, true);
    });
  });
});
