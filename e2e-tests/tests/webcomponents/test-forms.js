describe('webcomponents/forms.js', function() {
  var assert = require('assert'),
      helpers = require('../helpers.js'),
      forms = require('../../src/webcomponents/forms.js');
  
  helpers.initDOM();

  // --------------------
  // 
  // --------------------
  describe('blah', function() {
    it('should blah', function() {
      assert.equal(true, true);
    });
  });
});
